# 未完成任务与发布收口清单

更新时间：2026-09-16
适用版本：`0.6.0-stage6`
项目：邻里半径 · 15 分钟生活圈

## 一、当前结论

核心功能开发已经完成，项目现在处于“发布前收口”阶段。P1–P4、P5.1–P5.4、P6 和 P7 已形成可运行代码、测试、文档或离线证据；但正式提交尚未完成。

当前不能宣称完成的内容主要有：

1. 项目尚未从多项目父 Git 仓库中独立发布。
2. 当前工作区的阶段性改动尚未形成正式提交。
3. 真实社区独立人工核验数据尚未完成，因此准确率和召回率不可计算。
4. 在线逐点/批量基准尚未在授权 AK 和真实配额环境运行。
5. 远程 CI、完整 Gitleaks 历史扫描和 Docker 镜像实际构建尚未闭环。
6. 比赛提交信息、Release、演示地址和演示视频尚未填写或发布。
7. P5.1–P5.4 已实现为 `planning-scenario-v1` 确定性情景与模板化报告摘要；真实选址约束和独立核验仍未实现。它属于可选的产品加分项，不是当前核心链路的故障。

## 本轮执行进度（2026-09-16）

本轮已在父仓库之外创建独立发布副本：`D:\\baidumap-release`。该副本已通过严格仓库根目录检查，并形成本地提交（提交哈希以独立仓库 `git log -1` 为准）。

| 任务 | 本轮状态 | 证据 |
| --- | --- | --- |
| U-01 独立发布仓库 | 已完成 | `D:\\baidumap-release`；严格根目录检查通过 |
| U-02 当前阶段正式提交 | 已完成 | 独立仓库已形成多次本地提交，最新提交以独立仓库 `git log -1` 为准，工作区干净 |
| U-03 干净环境门禁 | 已完成 | `npm ci`、lint、36/36 前端测试、build、桌面/移动 E2E 2/2、后端 43/43、固定演示启动、提交自检和模板校验通过 |
| U-04 Docker / SBOM | 未完成 | 本机 Docker daemon 未运行，尚未进行实际镜像构建 |
| U-05 远程 CI / Gitleaks | 未完成 | 尚未创建或授权远程仓库；完整历史扫描未执行 |
| U-06/U-07/U-08 独立人工真值 | 未完成 | POI、边界和灰区数据仍为 `template` / `pending` |
| U-09 在线 RouteMatrix 基准 | 未完成 | 没有授权 AK 和可用配额环境 |
| U-10 提交元数据 | 未完成 | 比赛、仓库、Release、团队等信息仍待人工填写 |
| U-11 演示排练与录像 | 未完成 | 脚本已完成，排练次数和录像仍待人工完成 |
| P5.1–P5.3 规划情景能力 | 已完成 | `planning-scenario-v1` 已接入报告、HTML 导出和前端展示；仅为有边界情景估算 |

本轮完成的是本地可验证的发布基础，不代表远程发布、真实人工核验、在线基准或正式比赛提交已经完成。

## 二、当前验证基线

截至本清单更新时间，当前工作区已验证：

| 检查项 | 当前结果 | 说明 |
| --- | --- | --- |
| 前端 Node 测试 | 通过，36/36 | 业务逻辑、等时圈、报告、错误状态等测试通过 |
| 后端 Python 测试 | 通过，43/43 | 编译和单元测试通过 |
| 提交自检 | 通过 | 27 项交付物、固定样例和离线基准结构通过 |
| 模板数据校验 | 通过 | 30 条 POI 槽位、12 条边界槽位、12 条灰区槽位的模板结构通过 |
| Docker Compose 配置 | 通过 | 配置语法通过，但本机 Docker daemon 未运行 |
| `git diff --check` | 通过 | 只有 Windows 换行提示 |
| 严格独立仓库检查 | 未通过 | 当前 Git 根目录仍是 `D:\\cursor的项目`，不是本项目目录 |
| lint / build / E2E | 已通过（独立副本） | 独立副本重新安装依赖后，lint、build 和桌面/移动 E2E 2/2 通过 |

这里的“当前工作区未通过”不能直接等同于源码失败。正式发布前必须在独立干净副本重新安装依赖并重新执行完整门禁。

## 三、一级阻塞任务：正式发布前必须完成

### U-01 建立独立发布仓库

状态：已完成本地独立副本，尚未创建远程仓库

当前项目位于多项目父仓库中。父仓库还包含其他项目的改动，不能直接作为比赛仓库发布。

完成要求：

- 在父仓库之外创建新的项目目录。
- 只复制 `baidumap` 项目内容，不复制父仓库 `.git`、其他项目、真实 `.env`、`node_modules`、`dist` 和测试产物。
- 在独立目录初始化 Git。
- 执行并通过：

```powershell
npm run check:repository -- --require-independent-root
```

- 确认 `.github/workflows/ci.yml` 位于独立仓库根目录下。

完成证据：独立仓库路径、根目录检查输出、初始提交哈希。

参考：[repository-release.md](repository-release.md)。

可直接交给后续开发者执行的任务提示词见：[发布收口执行开发者提示词](developer-prompt-for-completion.md)。

### U-02 将当前阶段代码形成正式提交

状态：已完成本地正式提交，尚未推送远程

当前项目范围内有 25 个已修改文件和 56 个未跟踪文件，包含阶段 6 文档、CI、验证脚本、候选数据、测试和源码改动。

完成要求：

- 在独立仓库中检查文件清单和敏感文件排除规则。
- 确认没有提交真实 AK、`.env`、缓存、构建产物和测试产物。
- 完成一次或多次清晰的正式提交。
- 记录最终提交哈希。

注意：不要在当前多项目父仓库中执行无范围的 `git add .`、提交、重置或历史改写。

### U-03 在干净环境重跑前端和后端门禁

状态：已完成独立副本验证

当前 `npm test` 可以通过，但 `node_modules` 缺少 ESLint、Vite、React 和 Playwright 相关依赖，因此当前工作区的 lint、build 和 E2E 不能作为最终发布证据。

完成要求：

```bash
npm ci
npm run lint
npm test
npm run test:e2e
npm run build
```

```powershell
cd backend
python -m pip install -r requirements.txt
python -m compileall -q app
python -m unittest discover -s tests -p "test_*.py"
cd ..
```

同时执行：

```bash
npm run check:repository -- --require-independent-root
npm run check:submission
npm run evidence:validate-template
docker compose config --quiet
```

完成证据：干净环境日志、Node/Python 版本、测试结果和固定演示启动记录。

### U-04 完成 Docker 镜像构建与 SBOM 复核

状态：未完成，受 Docker daemon 阻塞

Compose 配置语法已经通过，但本机 Docker daemon 当前未运行，前端和后端镜像尚未实际构建。

完成要求：

- 启动 Docker daemon。
- 构建前端镜像和后端镜像。
- 验证容器间 `/api` 访问、健康检查和前端页面启动。
- 生成正式镜像 SBOM。
- 复核基础镜像和 OS 包许可证。
- 保存镜像标签、构建日志和 SBOM 文件。

### U-05 创建远程仓库并通过 CI / Gitleaks

状态：未完成，受远程仓库和授权阻塞

仓库中已有 CI 配置，但尚未创建或授权正式远程比赛仓库，也没有远程运行结果。

CI 应至少通过：

- 前端依赖安装、lint、单元测试、E2E 和生产构建。
- 后端依赖安装、编译和 43 个测试。
- 前后端 Docker 镜像构建。
- 完整 Git 历史 Gitleaks 扫描。

完成证据：远程仓库 URL、CI 运行 URL 或截图、Gitleaks 结果、Docker 构建结果。

参考：[CI 配置](../.github/workflows/ci.yml)。

## 四、二级阻塞任务：真实证据补齐

### U-06 完成 30 条 POI 独立人工核验

状态：未完成，当前数据仍为 `template`

目标数量：30 条 POI，菜市场、药店、小学各至少 5 条，且每条记录必须有独立来源或实地核验依据。

每条记录至少补齐：

- 名称、类别、地址、坐标。
- 是否营业或可用。
- 百度 POI UID（如有）。
- 独立核验来源、来源 URL 或实地记录编号。
- 核验日期、核验人和限制说明。

核验来源不能只使用同一次百度 Place Search 返回结果。优先使用政府名录、机构官网、官方电话记录或实地记录。

完成要求：

- 所有目标记录状态改为 `verified`。
- `metadata.json` 的 `dataset_status` 改为 `collected`。
- 运行严格验证器通过。
- 重新生成指标 JSON，并更新真实社区报告。

### U-07 完成 12 条独立边界样本核验

状态：未完成

当前已有 12 个方向的边界槽位，但仍是待填数据。

每条至少补齐：

- 系统预测耗时和坐标。
- 独立步行或独立路线来源。
- 实际步行耗时。
- 绝对误差、入口偏移和道路阻隔说明。
- 核验日期、核验人和数据限制。

完成后才可以计算边界耗时绝对误差。当前 12/12 的 RouteMatrix 复核属于同源内部复核，不能作为独立人工真值。

### U-08 完成 12 条灰区分层样本核验

状态：未完成

灰区样本应覆盖：

- 灰区内部样本。
- 灰区边缘样本。
- 非灰区对照样本。

需要使用独立 POI 清单和现场可达性判断确认“是否真实缺失设施”，不能只依据系统自己的预测结果。

完成后才可以计算灰区准确率和灰区召回率，并补充至少一个真实误差案例。

### U-09 在授权 AK 环境运行在线 RouteMatrix 基准

状态：脚本已完成，真实产物未完成

需要在已授权、配额可用且不绕过 QPS 限制的环境中运行：

- 逐点请求多轮基准。
- 批量请求多轮基准。
- 冷启动和缓存命中场景。
- 所有成功、失败、限流、超时和重试轮次。

不能只保留最好的结果，也不能用离线模拟结果替代在线结论。

完成证据：`artifacts/benchmarks/route-matrix-online.json`，其中应包含 `source=online-live`、运行环境、轮次、失败类型和限制说明。

参考：[engineering-evidence.md](engineering-evidence.md)。

## 五、三级任务：提交与演示收尾

### U-10 填写提交元数据

状态：未完成

需要人工填写：

- 比赛平台和截止时间。
- 独立仓库 URL。
- Release URL。
- 在线演示 URL。
- 团队名称、成员和分工。
- 最终版本标签和提交哈希。
- 固定样例 SHA-256。
- CI 运行 URL 或截图。
- 干净环境验收记录。
- 演示视频或 PPT 地址。

未知信息必须保持“待填写”，不能猜测或伪造。

参考：[submission-metadata.md](submission-metadata.md)。

### U-11 排练并录制三分钟演示

状态：脚本已完成，排练和录像未完成

完成要求：

- 至少完整排练 5 次。
- 准备在线模式和无 AK 离线模式两套流程。
- 记录使用的版本标签和固定样例哈希。
- 录制主视频和备用视频。
- 在提交元数据中记录视频/PPT 地址。

演示必须明确区分：真实在线数据、内部复核数据、固定样例和尚未完成的独立人工真值。

参考：[demo-script.md](demo-script.md)。

## 六、可选任务：P5 规划功能

P5 不属于当前发布收口的硬性阻塞，但如果需要继续增加产品亮点，可按以下顺序实现：

| 编号 | 功能 | 当前状态 |
| --- | --- | --- |
| P5.1 | 根据灰区面积、人口代理值和缺失类别生成规划优先级 | 已实现（确定性情景） |
| P5.2 | 生成候选设施类别/位置并估算覆盖改善量 | 已实现（上限情景，不是真实选址） |
| P5.3 | 增加规划前/规划后指标情景对比 | 已实现（情景估算，不是实测改善率） |
| P5.4 | 自然语言报告润色 | 已实现（确定性模板，不使用 LLM 判定） |

P5.1–P5.4 已完成，但必须继续保留“情景估算”边界；真实道路、用地、入口、服务半径和独立真值核验完成前，不得把结果包装成正式选址或真实改善结论。报告摘要是可审计的确定性模板，不改变评分或地图判定。

## 七、推荐执行顺序

```text
U-01 独立仓库
  → U-02 正式提交
  → U-03 干净环境门禁
  → U-04 Docker / SBOM
  → U-05 远程 CI / Gitleaks
  → U-06/U-07/U-08 独立人工真值
  → U-09 在线基准
  → U-10 提交元数据
  → U-11 演示排练与录像
  → （可选）真实选址约束及独立核验
```

## 八、完成定义

只有同时满足以下条件，才可以将项目状态改为“正式提交完成”：

- 独立仓库严格根目录检查通过。
- 干净环境前后端门禁全部通过。
- Docker 镜像构建、SBOM 和许可证复核完成。
- 远程 CI 和完整历史 Gitleaks 通过。
- 30 条 POI、12 条边界、12 条灰区独立核验完成，并成功生成指标。
- 在线基准结果已保存，且没有用离线结果冒充线上结果。
- 提交元数据、Release、演示地址和最终版本哈希已填写。
- 演示视频已完成并与最终版本一致。

在此之前，项目可以称为“核心功能完成、发布收口中”，不能称为“比赛正式交付完成”。
