# 阶段 2、QPS 与阶段 4 验收记录

验收日期：2026-09-13

## 阶段 2：真实 15 分钟边界

使用默认中心点 `116.284206, 40.051819`、真实百度步行 RouteMatrix 与 DirectionLite 单路线接口，关闭服务端响应缓存后执行：

- 目标耗时：900 秒；容差：±60 秒。
- 初始方向：36；自适应新增方向：2。
- 有效路线样本：274 / 274。
- 单路线几何截点：12 / 12；沿路线复核校准：2 轮。
- 最终内部 RouteMatrix 交叉复核：12 / 12 通过；实测范围 845–913 秒。该同源检查不冒充独立人工真值。
- 输出：有效 GeoJSON Polygon；高置信度；无算法警告。
- 冷启动完整耗时：91 秒。

该结果满足“边界验证点大部分落在 840–960 秒”的完成标准，并把抽检点覆盖提高到 12 / 12。

## QPS 节流

后端使用与并发信号量分离的滑动窗口限速器。默认配置为 `BAIDU_MAX_QPS=1`、`BAIDU_QPS_WINDOW_SECONDS=3.2`，缓存命中不消耗上游配额。

本轮真实验收数据：

- 28 个业务请求触发 29 次上游调用；缓存命中为 0。
- 限速器主动等待 28 次，累计等待 64,815 ms。
- 上游瞬时限额响应 1 次，自动重试 1 次；最终错误 0，熔断器未打开。
- 响应元数据会分别暴露 `rateLimitWaits`、`rateLimitWaitMs` 与 `upstreamRateLimits`，不会把限额事件隐藏为普通成功。

## 阶段 4：浏览器端到端

命令：`npm run test:e2e`

- 桌面 Chrome：通过。
- Pixel 5 手机视口：通过。
- 覆盖地图选点、10 分钟切换、完整分析进度、12 / 12 边界证据、图层开关、报告图表、HTML 下载、无横向溢出和 3 分钟时限。
- 两条主流程总计 10.7 秒，2 / 2 通过。

## 回归检查

- 前端单元测试：27 / 27。
- 后端单元测试：27 / 27。
- ESLint：通过。
- Vite 生产构建：通过。
- `docker compose config --quiet`：通过。
- `git diff --check`：通过，仅有仓库既有的 Windows 换行提示。
