# 默认使用离线样例，复制为 .env.local 后再切换在线模式
VITE_DATA_MODE=mock
VITE_API_BASE_URL=/api
# 严格 QPS 配额下，POI + 边界复核可超过 60 秒
VITE_API_TIMEOUT_MS=180000

# 仅放浏览器端地图 SDK AK；服务端 AK 必须留在后端环境变量中
VITE_BAIDU_BROWSER_AK=

# 开发时可将 /api 代理到 FastAPI，例如 http://localhost:8000
VITE_DEV_API_TARGET=http://localhost:8000
