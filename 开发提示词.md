# “15 分钟生活圈”智能体检与规划助手开发计划

## 1. 项目目标

基于百度地图开放能力开发一个可本地一键启动、可公开部署的 Web 应用。用户可以搜索地址、输入坐标或直接点击地图选择中心点，系统随后：

1. 基于真实步行路网计算 15 分钟（900 秒）可达区域，而不是绘制固定半径圆。
2. 检索并清洗生活圈内医疗、教育、购物、养老等民生设施 POI。
3. 输出生活圈热力图、设施覆盖统计、综合评分和可解释的体检报告。
4. 识别周边 1 公里内缺少菜市场、药店或小学的服务盲区，并在地图上标为灰色区域。
5. 给出有数据依据的设施补齐与社区规划建议。

项目必须满足比赛 PDF 中的源代码开源、一键构建/部署、自动化测试、技术设计文档、真实社区对比报告和 CI/CD 要求。

## 2. 评分导向与验收证据


| 评审项           | 权重  | 开发重点                                             | 演示/验收证据                                |
| ------------- | --- | ------------------------------------------------ | -------------------------------------- |
| 功能正确性与覆盖率     | 40% | 坐标转换、POI 检索、步行算路、真实路网等时圈、1 公里盲区识别                | 一次完整现场演示；保存 API 响应摘要、算法参数、地图图层和自动化验收结果 |
| API 深度调用与工程优化 | 30% | RouteMatrix 步行批量算路、并发控制、缓存、重试、限流降级、空间插值          | API 调用次数与耗时对比；异常注入测试；粗细采样效果对比          |
| 产品交互与用户体验     | 15% | 地图选点、进度反馈、图层开关、热力图/柱状图/雷达图、报告导出                  | 3 分钟无讲解可完成操作；桌面和移动端均可用                 |
| 开源工程规范        | 15% | 清晰目录、类型与格式检查、测试、Docker、README、许可证、GitHub Actions | 新环境按 README 一次启动成功；CI 全绿；仓库无明文 AK      |


原则：核心结论必须由确定性地图数据和算法生成；AI 文案只负责解释和建议，不参与“是否达标”的事实判断。

## 3. 技术方案



### 3.1 推荐技术栈

- 前端：React + TypeScript + Vite + 百度地图 JavaScript API 4.0。
- UI 与图表：Ant Design + ECharts；地图覆盖物使用百度地图原生 Polygon、Heatmap/自定义图层。
- 后端：Python 3.12 + FastAPI + httpx 异步客户端 + Pydantic。
- 空间计算：Shapely + NumPy；统一使用 GeoJSON 作为前后端空间数据格式。
- 缓存与示例数据：SQLite。缓存键包含接口名、标准化参数和算法版本，避免重复消耗配额。
- 测试：Pytest、Vitest、Playwright；外部 API 通过固定响应夹具测试。
- 工程化：Docker Compose、`.env.example`、GitHub Actions、Ruff、mypy、ESLint、Prettier。
- 开源许可证：Apache-2.0（保留 NOTICE，适合完整开源应用）；若参赛平台要求 MIT，再统一切换。



### 3.2 百度地图能力分工

- 浏览器 AK：只加载地图底图和前端交互，配置白名单。
- 服务端 AK：调用地理编码/逆地理编码、坐标转换、地点检索 3.0、步行批量算路；只保存在服务端环境变量中。
- 所有内部坐标统一转换为 BD-09，经纬度字段统一为 `lat`、`lng`，请求适配层负责百度接口的参数顺序。
- RouteMatrix 步行批量算路按“一个中心点 + 最多 50 个终点”分批；真实批次上限以开发时账号控制台和官方接口返回为准。



### 3.3 总体架构

```text
React Web
  ├─ 地图选点 / 地址搜索 / 参数设置
  ├─ 等时圈、POI、盲区和建议图层
  └─ 评分卡、柱状图、雷达图、报告页
          │ REST + GeoJSON
FastAPI
  ├─ BaiduMapClient：鉴权、重试、限流、批量请求
  ├─ IsochroneEngine：采样、批量算路、900 秒边界搜索
  ├─ PoiPipeline：检索、分页、分类、去重和质量标记
  ├─ BlindSpotEngine：1 公里覆盖判定与灰区聚合
  ├─ ScoringEngine：确定性评分与建议证据
  └─ SQLite Cache / Sample Snapshot / Metrics
          │
百度地图 Web Service API
```



## 4. 核心算法定义



### 4.1 15 分钟步行等时圈

1. 以中心点为起点，在 360° 上先生成 36 个方向，每 10° 一条射线。
2. 每条射线生成多个候选终点，初始距离建议为 300、600、900、1200、1500 米。
3. 使用步行 RouteMatrix 批量获取中心点到候选点的真实路网耗时。
4. 对跨越 900 秒的区间执行二分/线性插值，直至边界耗时误差不超过 60 秒或空间误差不超过 50 米。
5. 若相邻方向的边界半径差超过 30%，在中间补充方向进行自适应加密，最多扩展到 72 个方向。
6. 按方位角连接边界点，使用几何修复消除自交，并用轻量平滑生成 GeoJSON Polygon。
7. 同时保留每个采样点的 `duration`、`distance`、`status` 和来源批次，供热力渲染和算法解释。
8. 对中心点、四个主方向和凹陷区域抽取若干点调用单路线规划复核，避免批量算路异常导致错误边界。

等时圈结果必须标明置信度：`high`（有效方向不少于 90%）、`medium`（不少于 70%）、`low`（使用了明显降级）。低置信度结果不能伪装成精确结果。

### 4.2 POI 检索与清洗

- 核心统计类别：菜市场/生鲜超市、药店、社区医疗、综合医院、小学、幼儿园、养老服务、便利店/超市。
- 必测盲区类别：菜市场、药店、小学。
- 检索半径覆盖“中心点周边 2 公里包围盒”，确保盲区点做 1 公里邻域判断时不漏掉分析边界外的设施。
- 对每类关键词分页拉取，记录 `uid`、名称、地址、类别、坐标、来源关键词和抓取时间。
- 首先按百度 POI `uid` 去重；无 uid 时按“标准化名称 + 50 米距离”去重。
- 用类别词典进行二次归类，排除培训机构、成人学校、保健品店等误召回项；不确定项标为 `needs_review`。
- 对比报告必须记录检索时间、关键词词典版本、页数、原始数量、去重数量和排除数量。



### 4.3 服务盲区识别

1. 在中心点周边 1 公里分析范围内生成约 100 米间距的六边形或方形网格候选点。
2. 分别判断每个候选点 1 公里内是否存在菜市场、药店、小学。
3. 初筛使用球面距离和空间索引；接近 1 公里阈值的边界点，再用步行批量算路复核真实可达距离。
4. 任一必测类别缺失即标记该点为盲点，并记录缺失类别；三类都缺失的点使用更深灰色。
5. 将相邻盲点聚合、平滑为灰色 GeoJSON 区域，同时保留原始盲点用于审计。
6. 页面清晰区分“15 分钟等时圈”和“1 公里设施盲区”，避免两个口径混淆。



### 4.4 覆盖评分与规划建议

- 覆盖评分至少包含：设施种类完整度、15 分钟圈内数量、最近步行耗时、空间均衡度、盲区占比。
- 所有阈值写入带版本的配置文件，不散落在代码中；报告展示评分公式和数据证据。
- 建议引擎先使用可审计规则，例如“西北方向药店盲区连续面积超过阈值，建议优先在候选质心附近补点”。
- 可选接入大模型将结构化结论改写成自然语言，但断网或未配置模型密钥时，规则模板仍能生成完整报告。



## 5. 开发任务与完成标准



### P0 - 赛题冻结与项目骨架

- [ ] `T0.1` 建立需求追踪矩阵，把 PDF 每一条要求映射到功能、测试和文档章节。
- [ ] `T0.2` 建立前后端单仓目录、配置加载、日志、统一错误格式和健康检查。
- [ ] `T0.3` 加入 Apache-2.0、`.gitignore`、`.env.example`、贡献指南和安全说明。
- [ ] `T0.4` 提供 Docker Compose 和本地一键启动命令。
- [ ] `T0.5` 选择一个便于实地核验的真实社区；冻结中心点、验证范围和对照点位。

完成标准：无 AK 时可用样例快照启动；有 AK 时健康检查能验证地图服务配置；仓库中扫描不到真实密钥。

### P1 - 百度地图接入层

- [ ] `T1.1` 实现地理编码、逆地理编码和坐标转换适配器。
- [ ] `T1.2` 实现地点检索 3.0 的圆形/多边形检索、分页和状态码映射。
- [ ] `T1.3` 实现步行 RouteMatrix 批量请求与单路线复核。
- [ ] `T1.4` 实现超时、指数退避、随机抖动、QPS 并发门控和熔断。
- [ ] `T1.5` 实现请求缓存、请求 ID、耗时和配额统计；日志对 AK 脱敏。

完成标准：三类 API 均有正常、空结果、超时、限流、服务错误和畸形响应测试；批量请求不超过接口上限。

### P2 - 等时圈计算引擎

- [ ] `T2.1` 实现极坐标候选点生成与 BD-09 坐标计算。
- [ ] `T2.2` 实现批量测时、900 秒边界搜索、自适应方向加密。
- [ ] `T2.3` 实现异常方向插值、离群点检测、自交修复和 GeoJSON 输出。
- [ ] `T2.4` 生成热力采样数据与置信度指标。
- [ ] `T2.5` 建立圆形空旷路网、河流阻隔、道路绕行、API 部分失败四组算法测试。

完成标准：固定快照输入产生稳定多边形；边界验证点的步行耗时大部分落在 840-960 秒；道路阻隔方向明显收缩；局部接口失败仍能返回带置信度的结果。

### P3 - POI 与盲区分析

- [ ] `T3.1` 建立民生设施关键词与归类词典。
- [ ] `T3.2` 实现多页 POI 拉取、标准化、去重、误召回过滤和质量统计。
- [ ] `T3.3` 实现生活圈内 POI 空间筛选与各类别覆盖指标。
- [ ] `T3.4` 实现 1 公里候选网格、三类设施缺失判断和边界复核。
- [ ] `T3.5` 将盲点聚合为灰区多边形，输出缺失类别、面积和人口代理指标的扩展接口。

完成标准：POI 重复项不会重复计数；可准确复现人工构造的三种盲区；页面可查看每个灰区的判定证据。

### P4 - 产品界面与可视化

- [ ] `T4.1` 完成地图主页：地址搜索、坐标输入、地图点击选点、15/10/20 分钟参数切换。
- [ ] `T4.2` 完成分析进度条和阶段提示，避免长计算期间页面无反馈。
- [ ] `T4.3` 渲染等时圈边界、耗时热力、分类 POI、灰区和候选补点；提供图层开关和图例。
- [ ] `T4.4` 完成体检报告：总分、分类卡片、柱状图、雷达图、盲区列表和证据解释。
- [ ] `T4.5` 完成响应式布局、空状态、错误恢复、键盘操作和色弱可辨识配色。
- [ ] `T4.6` 支持报告导出为打印友好的 HTML/PDF 页面，导出内容包含算法版本和数据时间。

完成标准：首次用户在 3 分钟内完成选点、分析、查看盲区和导出报告；桌面与手机浏览器主流程均无阻塞。

### P5 - 规划助手与比赛亮点

- [ ] `T5.1` 根据灰区面积、缺失类别和周边覆盖生成可解释的补点优先级。
- [ ] `T5.2` 在灰区内生成候选设施位置，并估算新增设施后的盲区减少比例。
- [ ] `T5.3` 增加“规划前/规划后”地图和指标对比。
- [ ] `T5.4` 可选加入自然语言报告润色，确保未配置额外 AI 服务时仍可完整演示。

完成标准：每条建议均可回溯到缺失类别、候选位置和预计改善数值；不能输出无数据依据的建议。

### P6 - 测试、性能与降级

- [ ] `T6.1` 单元测试覆盖算法、坐标顺序、分类词典、缓存键和错误映射。
- [ ] `T6.2` 契约测试覆盖百度 API 响应结构，所有网络测试均可用录制快照离线运行。
- [ ] `T6.3` Playwright 覆盖完整演示流程、图层切换、失败重试和报告导出。
- [ ] `T6.4` 建立性能基线，对比逐点请求与批量算路的调用次数、耗时和成功率。
- [ ] `T6.5` 做故障注入：无 POI、429/QPS 限流、5xx、超时、部分方向失败、无效 AK。
- [ ] `T6.6` 降级顺序：命中缓存 → 降低采样密度 → 对缺失方向插值 → 使用已提交样例快照；页面始终标明数据状态。

完成标准：核心后端覆盖率目标不低于 80%；CI 中单测、类型检查、格式检查和前端构建全部通过；所有降级均有界面提示。

### P7 - 文档、真实对比报告与提交

- [ ] `T7.1` README：产品截图、快速开始、环境变量、AK 类型与白名单、Docker、测试、常见问题。
- [ ] `T7.2` 技术设计：API 策略、等时圈算法、POI 清洗、盲区算法、缓存/并发/降级和安全边界。
- [ ] `T7.3` 真实社区报告：中心点、采集时间、人工核验方法、算法结果、地图截图、误差案例与限制。
- [ ] `T7.4` 准备固定示例数据和一键演示脚本，确保评委无 AK 或网络波动时也可跑通。
- [ ] `T7.5` 配置 GitHub Actions：后端检查与测试、前端检查与构建、Docker 镜像构建、密钥扫描。
- [ ] `T7.6` 完成开源合规检查、第三方依赖许可证清单和最终提交清单。
- [ ] `T7.7` 准备 3 分钟演示脚本：问题 → 选点 → 等时圈 → 设施体检 → 灰区 → 规划前后对比 → 工程亮点。

完成标准：在全新机器上仅根据 README 可启动；固定样例和在线模式都能演示；提交物逐条对应比赛要求。

## 6. 推荐执行顺序


| 阶段     | 任务      | 目标产物             | 退出条件        |
| ------ | ------- | ---------------- | ----------- |
| 第 1 阶段 | P0 + P1 | 可运行骨架、百度 API 客户端 | 三类地图能力调用通过  |
| 第 2 阶段 | P2      | 真实路网 15 分钟等时圈    | 算法测试和地图初显通过 |
| 第 3 阶段 | P3      | POI 体检与灰色盲区      | 必测三类盲区可审计   |
| 第 4 阶段 | P4      | 完整交互与报告          | 主流程可演示      |
| 第 5 阶段 | P5 + P6 | 规划亮点、性能和容错       | 异常场景与性能基线通过 |
| 第 6 阶段 | P7      | 文档、CI、真实报告、演示    | 提交清单全部勾选    |


关键路径为 `P0 → P1 → P2 → P3 → P4 → P6 → P7`。P5 是加分项，但不能挤占核心正确性、测试和文档时间。

## 7. 质量门禁

每个阶段结束都必须同时满足：

1. 功能有自动化测试，不只依靠页面截图。
2. 外部 API 失败时有明确状态和恢复方式，不吞错、不伪造成功。
3. 新增算法参数进入统一配置并记录版本。
4. 用户可在报告中看见数据时间、样本数量、置信度和限制。
5. 真实 AK 不进入源码、日志、示例数据、前端包或 Git 历史。
6. 文档与代码在同一提交中更新。



## 8. 真实社区验证方案

- 选择一个能实地或通过公开资料核验的社区，并另外选一个道路阻隔明显的对照中心点。
- 人工记录至少 30 个设施，其中菜市场、药店、小学各不少于 5 个；记录名称、位置、类别、是否营业和核验来源。
- 随机抽取至少 12 个方向边界点，人工在百度地图步行规划中核对 15 分钟边界。
- 报告四类指标：POI 查全率、分类准确率、边界耗时误差、盲区准确率/召回率。
- 对桥梁、封闭小区、施工、入口偏移和 POI 分类错误建立案例表，明确当前限制，不只展示成功样例。



## 9. 开发开始前的输入与默认策略

后续正式开发需要：

- 百度地图浏览器端 AK。
- 百度地图服务端 AK，以及该账号的配额/QPS 信息。
- 参赛团队希望用于真实报告的城市和社区；若暂未确定，先使用可替换的示例中心点和离线快照开发，不阻塞核心功能。
- 比赛截止日期和最终提交平台；PDF 本身未给出这两项。

在上述信息不完整时，先完成无密钥项目骨架、Mock/快照模式、算法单测和界面框架，等 AK 与社区确定后再切换在线数据。

## 10. 明确不做

- 不用固定 1 公里圆冒充 15 分钟真实路网等时圈。
- 不在浏览器代码中暴露服务端 AK。
- 不把大模型输出当作设施覆盖或盲区的事实依据。
- 不抓取百度地图网页或绕过官方 API 配额。
- 不为追求视觉效果省略置信度、数据时间和异常状态。
- 不在核心功能未稳定前开发账号系统、社交分享等与评分无关的功能。
